# Global headers
# Note: X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy,
# HSTS, and CSP are all set in src/hooks.server.ts so they apply at runtime.
# The entries below serve as a CDN-level fallback and apply to static assets
# that are served directly by Cloudflare without invoking the Worker.
/*
  X-Frame-Options: SAMEORIGIN
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: accelerometer=(), camera=(), geolocation=(), gyroscope=(), magnetometer=(), microphone=(), payment=(), usb=()
  # X-XSS-Protection is deliberately omitted — it is deprecated and removed from all
  # modern browsers. Including it can cause false-positive page blocks on old IE.

# Cache static assets
/css/*
  Cache-Control: public, max-age=31536000, immutable

/assets/*
  Cache-Control: public, max-age=31536000, immutable

# Cache HTML files for shorter time
/*.html
  Cache-Control: public, max-age=3600, must-revalidate

# Don't cache the root index
/index.html
  Cache-Control: public, max-age=0, must-revalidate
